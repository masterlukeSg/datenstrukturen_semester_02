#include <iostream>

namespace ProjectAlpha
{

    template <class X>
    class gerodneteMenge
    {
        virtual void insert(K) = 0;
        virtual bool find(K) = 0;
        virtual remove(K) = 0;

        

    };

}